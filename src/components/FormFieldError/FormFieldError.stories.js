import FormFieldError from './index.vue';

export default { title: 'FormFieldError' };

export const Default = () => ({
  components: { FormFieldError },
  template: '<form-field-error>Required field</form-field-error>',
});
